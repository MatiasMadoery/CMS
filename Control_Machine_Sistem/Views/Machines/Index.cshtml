@model Pager<Control_Machine_Sistem.Models.Machine>

@{
    ViewData["Title"] = "Máquinas";
}

<h1 class="mb-4">Productos Vendidos</h1>

<form method="get" id="filter-form" class="mb-4">
    <div class="row">
        <div class="col-md-3">
            <input type="text" name="searchString" class="form-control" placeholder="Buscar cliente o modelo..." value="@ViewData["searchString"]" />
        </div>
        <div class="col-md-3">
            <button type="submit" class="btn btn-primary">Buscar</button>
        </div>
         <div class="col-md-3">
            @Html.DropDownList("categoryId", (SelectList)ViewBag.Categories, "Filtrar por producto...", new { @class = "from-control"})
            <button type="submit" class="btn btn-primary">Buscar</button>
        </div>        
    </div>
</form>
<div class="mb-3">
    <a asp-action="Create" class="btn btn-success">Cargar nueva venta</a>
</div>

<hr />
@if (!Model.Elements.Any())
{
    <div class="alert alert-warning" role="alert">
        No se encontraron resultados para tu búsqueda.
    </div>
}
else
{
    <table class="table table-striped table-bordered">
        <thead class="table-light">
            <tr>
                <th>@Html.DisplayNameFor(model => model.Elements.First().Customer!.Name)</th>
                <th>@Html.DisplayNameFor(model => model.Elements.First().Model.Category.Name)</th>
                <th>@Html.DisplayNameFor(model => model.Elements.First().Model!.Name)</th>
                <th>@Html.DisplayNameFor(model => model.Elements.First().DeliveryDate)</th>
                <th>@Html.DisplayNameFor(model => model.Elements.First().WarrantyExpirationDate)</th>

                <th>Documentación</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Elements)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.Customer!.Name)</td>
                    <td>@Html.DisplayFor(modelItem => item.Model!.Category.Name)</td>
                    <td>@Html.DisplayFor(modelItem => item.Model!.Name)</td>
                    <td>@item.DeliveryDate?.ToString("dd/MM/yyyy")</td>
                    <td>@item.WarrantyExpirationDate?.ToString("dd/MM/yyyy")</td>
                    <td>
                        @if (item.DocUrls != null && item.DocUrls.Any())
                        {
                            <ul>
                                @foreach (var doc in item.DocUrls)
                                {
                                    <li class="document-name" data-url="@doc">
                                        <a href="@doc" download>@doc</a>
                                    </li>
                                }
                            </ul>
                        }
                        else
                        {
                            <span>Sin documentos asociados </span>
                        }
                    </td>
                    <td>
                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-info">Detalle</a>
                        <a asp-action="GenerateQr" asp-controller="QrCodes" asp-route-machineId="@item.Id" class="btn btn-warning">Generear Qr</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}

<div class="mt-3">
    @if (Model.PreviousPage)
    {
        <a asp-action="Index" 
        asp-route-page="@(Model.CurrentPage - 1)" 
        asp-route-searchString="@ViewData["searchString"]"
           asp-route-categoryId="@ViewData["categoryId"]"
        class="btn btn-secondary btn-sm">Anterior</a>
    }

    Página @Model.CurrentPage de @Model.TotalPages

    @if (Model.NextPage)
    {
        <a asp-action="Index" 
        asp-route-page="@(Model.CurrentPage + 1)" 
        asp-route-searchString="@ViewData["searchString"]"
        asp-route-categoryId="@ViewData["categoryId"]"
        class="btn btn-secondary btn-sm">Siguiente</a>
    }
</div>

<script src="~/js/views.js"></script>
